Place font files (TTF/OTF/WOFF) here if you want to bundle fonts with the template.
Recommended open-source pair: Source Serif 4 (body) and Source Sans 3 (headings).

If you place them here, uncomment the `import-font(...)` lines in `book.typ`.
